{% extends 'base.html.twig' %}

{% block main %}
    <div class="container">
        {{ discipline.nom }} <br />
        {% for cours in courses %}
            <a href="{{ path('oneCours', {id: cours.id}) }}" class="label label-info">{{ cours.nom }}</a>
        {% endfor %}
    </div>
{% endblock %}