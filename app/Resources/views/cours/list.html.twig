{% extends 'base.html.twig' %}

{% block main %}
    Filter by :
    {% for course in disciplines %}
        <a href="" class="label label-info">{{ course.discipline }}</a>
    {% endfor %}

    <div class="container">
        {% for course in courses %}
            {{ course.title }} | <a href="{{ path('oneItem', {id: item.id}) }}">View</a> {% if app.user.hasRole('ROLE_ADMIN') %}| <a href="{{ path('removeItem', {id: item.id}) }}">Remove</a>{% endif %}<br />
        {% endfor %}
    </div>
{% endblock %}