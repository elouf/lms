{% extends 'base.html.twig' %}

{% block vueAdmin %}
    {% if app.user.hasRole('ROLE_SUPER_ADMIN') %}
        <li><a href="{{ path('oneCours', {id: cours.id, mode: 'admin'}) }}">Vue admin</a></li>
    {% endif %}
{% endblock %}

{% block pageHeader %}
    <!--<div class="page-header">
        <div class="container">
            <h1>Détail des cours</h1>
            <ul class="breadcrumb">
                <li><a href="{{ path('homepage') }}">Accueil</a></li>
                <li class="active">Détail des cours</li>
            </ul>
        </div>
    </div>-->
{% endblock %}

{% block main %}

    <script>

        jQuery(document).ready(function () {



            $('.btnOpenDevoir').click(function(){
                var idItem = $(this).closest('[data-iditem]').attr('data-iditem');

                $.ajax({
                    type: 'POST',
                    url: "{{path('getDevoir_ajax')}}",
                    async: false,
                    dataType: "json",
                    data: {
                        id: idItem,
                        userId: $('[data-userId]').attr('data-userId')
                    },
                    success: function (data) {
                        $('.modal').modal();
                        $('.modal .modal-body').hide();
                        var popup = $('.modal .modal-body[data-contentModal=devoirDisplay]');
                        popup.attr('data-idItem', $(this).closest('.courseElem').attr('data-idItem')).show();

                        var dureeH = parseInt(data['duree'] / 3600);
                        var dureeM = parseInt((data['duree'] % 3600) / 60);
                        if(dureeM < 10){
                            dureeM = "0"+dureeM;
                        }

                        popup.find('.popupDevoirDuree').text(dureeH+'h'+dureeM);
                        popup.find('.popupDevoirNom').text(data['nom']);
                        popup.find('.popupDevoirDescription').html(data['description']);

                        var dateD = toJSDate(data['dateDebut']['date']);
                        var dateF = toJSDate(data['dateFin']['date']);
                        var limitDate = dateF;
                        limitDate.setHours(limitDate.getHours() + parseInt(dureeH));
                        limitDate.setMinutes(limitDate.setMinutes() + parseInt(dureeM));


                        popup.find('.alert').hide();

                    },
                    error: function(xhr, status, error) {
                    }
                });
            });


            function toJSDate(phpDateTime){
                var dateTime = phpDateTime.split(" ");//dateTime[0] = date, dateTime[1] = time

                var date = dateTime[0].split("-");
                var time = dateTime[1].split(":");

                return new Date(date[0], (date[1]-1), date[2], time[0], time[1], 0, 0);
            }
        });
    </script>

    <div class="container mt-140">
        <div class="row">
            <div class="col-sm-4">
                <div class="course-intro">
                    <img src="{{ asset('images/') }}{{ cours.imgFilePath }}" class="img-responsive" alt="">
                </div>
            </div>
            <div class="col-sm-8">
                <div class="course-full">
                    <h2 class="heading">{{ cours.nom }}</h2>
                    <div class="personal">
                        <!--<div>
                            <span>Enseignant</span>
                            <span>M. Lefebre <a href="teacher-profile.html"><i class="fa fa fa-user"></i> </a></span>
                        </div>
                        <div>
                        </div>-->
                    </div>
                    <div class="editor">
                        {{ cours.accueil | raw }}
                    </div>
                </div>
            </div>
        </div>

        <div class="clearfix"></div>

        <div class="row mt-40 mb-100">
            <div class="col-sm-3">
                <ul class="nav nav-pills nav-stacked pull-left" role="tablist" style="padding-bottom: 5px;padding-top: 5px">
                    {% for key,val in zonesSections if val.section.isVisible == 1 %}
                        <li role="presentation" {% if loop.index == 1 %} class="active" {% endif %}>
                            <a href="#t1body{{ val.section.id }}" aria-controls="t1body{{ val.section.id }}" role="tab" data-toggle="tab" aria-expanded="true"><i class="fa fa-pencil fa-fw" aria-hidden="true"> </i>{{ val.section.nom }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
            <div class="col-sm-9">
                <div class="tab-content">
                    {% set firstDisplayed = 1 %}
                    {% for key,val in zonesSections if val.section.isVisible %}
                        <div role="tabpanel" class="tab-pane fade{% if loop.index == 1 %} in active {% endif %}" id="t1body{{ val.section.id }}">
                            <div class="editor">
                                <ul class="list-default">
                                    {% for keyR,valR in val.zones.content %}
                                        {% if val.zones.containers[keyR].isVisible %}
                                            {% if val.zones.type[keyR] == "lien" %}
                                                <!--
                                                        LIEN
                                                -->
                                                <li data-idZone="{{ val.zones.containers[keyR].id }}"
                                                    class="courseElem lien">
                                                    <div class="row">

                                                        <div class="itemInfos itemHeader col-xs-10">
                                                            <div class="itemTitle">{{ valR.nom }}</div>
                                                            <div class="itemDescription">{{ valR.description | raw }}</div>
                                                        </div>
                                                        <div class="col-xs-2 text-right">
                                                            <a class="btn btnLien btn-sm btn-warning" target="_blank" href="{{ valR.url }}">
                                                                <i class="fa {{ valR.typeLien.faIcon }}"></i>
                                                            </a>
                                                        </div>

                                                    </div>


                                                </li>
                                            {% elseif val.zones.type[keyR] == "devoir" %}
                                                <!--
                                                        DEVOIR
                                                -->
                                                <li data-idZone="{{ val.zones.containers[keyR].id }}"
                                                    class="courseElem devoir" data-idItem="{{ valR.id }}">
                                                    <div class="">
                                                        <div class="row">
                                                            <div class="itemHeader col-xs-9 mb-10">
                                                                <div class="itemTitle mb-10">
                                                                    {{ valR.nom }}
                                                                </div>
                                                                <div class="ml-10">
                                                                    Du {{ valR.dateDebut|localizeddate('none', 'none', null, null, 'd LLLL Y à h:mm' ) }} au {{ valR.dateFin|localizeddate('none', 'none', null, null, 'd LLLL Y à h:mm' ) }}
                                                                </div>
                                                                <div class="ml-10">
                                                                    Durée : {{ valR.duree//3600  }}h{{ (valR.duree - 3600*(valR.duree//3600))//60 }}min
                                                                </div>
                                                                <div class="itemDescription ml-10">
                                                                    {{ valR.description | raw }}
                                                                </div>
                                                            </div>
                                                            <!-- On permet d'afficher la popup seulement si la date de fin n'est pas dépassée et si une copie n'a pas encore été déposée -->
                                                            {% if val.zones.copieFichier[keyR] == "undefined" and  "now"|date('Ymd H:i:s') < valR.dateFin|date('Ymd H:i:s') %}
                                                                <div class="col-xs-3 text-right">
                                                                    <div class="btn btn-sm btn-info btnOpenDevoir">Ouvrir</div>
                                                                </div>
                                                            {% endif %}
                                                        </div>
                                                        {% if val.zones.copie[keyR] != "undefined" or "now"|date('Ymd H:i:s') > valR.dateFin|date('Ymd H:i:s') %}
                                                            <!-- On affiche les fichiers (sujet, copie, corrigé) que si le user
                                                                 a déjà déposé une copie ou si la date de fin est dépassée-->
                                                            <table class="devoirCptRenduTable">
                                                                <tr>
                                                                    <td>Sujet du devoir</td>
                                                                    <td>
                                                                        {% for keyS,valS in val.zones.sujet[keyR] %}
                                                                            <a class="btn btn-sm btn-info mb-3" href="{{ valS.url }}">{{ valS.nom }}</a>
                                                                        {% endfor %}
                                                                    </td>
                                                                </tr>
                                                                {% if val.zones.corrige[keyR] != "undefined" %}
                                                                    <tr>
                                                                        <td>Corrigé type du devoir</td>
                                                                        <td>
                                                                            {% for keyC,valC in val.zones.corrigeType[keyR] %}
                                                                                <a class="btn btn-sm btn-success mb-3" href="{{ valC.url }}">{{ valC.nom }}</a>
                                                                            {% endfor %}
                                                                        </td>
                                                                    </tr>
                                                                {% endif %}
                                                                {% if val.zones.copie[keyR] != "undefined" %}
                                                                <tr>
                                                                    <td>Devoir commencé le {{ val.zones.copie[keyR].dateCreation|localizeddate('none', 'none', null, null, 'd LLLL Y à h:mm' ) }}</td>
                                                                    <td>
                                                                        {% if val.zones.copieFichier[keyR] != "undefined" %}
                                                                            <a class="btn btn-sm btn-warning" href="{{ val.zones.copieFichier[keyR].url }}">Ma copie</a>
                                                                        {% endif %}
                                                                    </td>
                                                                </tr>
                                                                {% endif %}
                                                                {% if val.zones.corrige[keyR] != "undefined" %}
                                                                    <tr>
                                                                        <td>Corrigé déposé le {{ val.zones.corrige[keyR].dateRendu|localizeddate('none', 'none', null, null, 'd LLLL Y à h:mm' ) }} par <a href="mailto:{{ val.zones.corrige[keyR].auteur.email }}">{{ val.zones.corrige[keyR].auteur.firstname }} {{ val.zones.corrige[keyR].auteur.lastname }}</a></td>
                                                                        <td>
                                                                            {% if val.zones.corrigeFichier[keyR] != "undefined" %}
                                                                                <a class="btn btn-sm btn-success" href="{{ val.zones.corrigeFichier[keyR].url }}">Ma copie</a>
                                                                            {% endif %}
                                                                        </td>
                                                                    </tr>
                                                                {% endif %}
                                                            </table>
                                                        {% endif %}
                                                    </div>

                                                </li>
                                            {% elseif val.zones.type[keyR] == "groupe" %}
                                                <!--
                                                        GROUPE
                                                -->
                                                <li data-idZone="{{ val.zones.containers[keyR].id }}"
                                                    class="courseElem groupsRessource">
                                                    <div class="row">
                                                        <div class="itemHeader itemTitle col-lg-5">{{ val.zones.groupe[keyR].nom }}</div>
                                                        <div class="col-lg-7 text-right">
                                                        {% for keyG,valG in val.zones.content[keyR] %}
                                                            <a class="btnLienGroupeRessource btn btn-xs"
                                                               style="background-color: {{ valG.categorieLien.couleur }};" target="_blank"
                                                               href="{{ valG.lien.url }}">
                                                                <i class="fa {{ valG.lien.typeLien.faIcon }} fa-2x"></i>
                                                                {{ valG.nom }}
                                                            </a>
                                                        {%  endfor %}
                                                        </div>
                                                    </div>
                                                </li>
                                            {% elseif val.zones.type[keyR] == "libre" %}
                                                <!--
                                                        LIBRE
                                                -->
                                                <li data-idZone="{{ val.zones.containers[keyR].id }}"
                                                    class="courseElem freeRessource">
                                                    <div class="">{{ val.zones.content[keyR].description | raw }}</div>
                                                </li>
                                            {% endif %}
                                        {% endif %}
                                        <hr class="mt-10 mb-10" />
                                    {% endfor %}
                                </ul>
                            </div>
                        </div>
                    {% endfor %}

                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" tabindex="-1" role="dialog">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>

                <!--                  POPUP DEVOIR                     -->

                <div class="modal-body" data-contentModal="devoirDisplay" data-idItem="" style="background:#aaa; background-size:cover">
                    <div class="row">
                        <div class="bginverse">
                            <div class="">
                                <div class="popupDevoirNom"></div>
                                <div class="popupDevoirDescription mb-10"></div>

                                <div class="alert alert-danger mb-10 alert_beforeDownload">
                                    <strong>Attention!</strong> Lors du démarrage du téléchargement, le chronomètre du devoir sera lancé.
                                    <p>Vous disposerez alors de <span class="popupDevoirDuree"></span> pour rendre vos fichiers.</p>
                                </div>

                                <div class="chronometre">
                                    <span class="chrono_h"></span>:<span class="chrono_m"></span>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}